<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://kev-hanwen-yang.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://kev-hanwen-yang.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-07T23:56:48+00:00</updated><id>https://kev-hanwen-yang.github.io/feed.xml</id><title type="html">Hanwen Yang</title><subtitle>Research notes on self-supervised video representation learning, JEPA-style latent world models, and embodied intelligence. </subtitle><entry><title type="html">Diagnosing LeWM’s Long-Horizon Rollouts: Stable Latents, Wrong Futures</title><link href="https://kev-hanwen-yang.github.io/blog/2026/lewm-latent-dynamics/" rel="alternate" type="text/html" title="Diagnosing LeWM’s Long-Horizon Rollouts: Stable Latents, Wrong Futures"/><published>2026-06-04T00:00:00+00:00</published><updated>2026-06-04T00:00:00+00:00</updated><id>https://kev-hanwen-yang.github.io/blog/2026/lewm-latent-dynamics</id><content type="html" xml:base="https://kev-hanwen-yang.github.io/blog/2026/lewm-latent-dynamics/"><![CDATA[<p><em>Reproduction details and the full diagnostic set: see the <a href="#reproducibility-and-references">Reproducibility and references</a> below.</em></p> <h2 id="abstract">Abstract</h2> <p>Latent world models are attractive for planning because they predict future states directly in a compact representation — potentially allowing the model to focus more capacity on predictive dynamics rather than pixel-level reconstruction. However, successful planning demands more than producing latents that look statistically well-behaved across the dataset: the predicted latents must also preserve task-relevant physical state when rolled out open-loop over long horizons.</p> <p>I study this question in LeWM, an end-to-end JEPA-style latent world model stabilized by a distributional regularizer called SIGReg, using the released Push-T checkpoint. Across 1000 test episodes, I roll out the predictor open-loop for 18 predictor steps at frame-skip 5 and evaluate both decoded physical quantities and latent geometry. The predicted latents remain globally well-formed: their norms stay within the encoder reference shell throughout rollout.</p> <p>But this distributional stability hides substantial dynamical failure. Physical-state probes degrade systematically with horizon, and the predicted latents themselves drift from the true encoded futures — across rollout horizons, they are 4.5–11× farther from the empirical encoded manifold than true future latents, move 4–6× too slowly, and follow trajectories that are systematically straighter than real encoded sequences. A teacher-forced control isolates the mechanism: even when the predictor is fed real encoded context latents at every horizon, its one-step velocity remains only about one-third of the true latent velocity, with the predicted step decomposing into roughly one-quarter forward progress along the true direction of motion plus comparable perpendicular drift.</p> <p>This suggests that LeWM’s long-horizon degradation is not merely an artifact of off-manifold feedback, but reflects an intrinsic conditional-mean smoothing bias in the one-step latent dynamics. The broader implication is that SIGReg-style distributional regularization can stabilize latent distributional shape without enforcing local dynamical fidelity, leaving room for architectural approaches that explicitly enforce persistent action-relevant state.</p> <h2 id="why-long-horizon-rollouts-matter">Why long-horizon rollouts matter</h2> <p>A latent world model can perform well on short-horizon prediction and still fail as a long-horizon predictor. For planning, the relevant question is not only whether the next predicted latent looks reasonable. It is whether a sequence of predicted latents, rolled forward many steps without correction from the encoder, continues to preserve the physical state needed to choose good actions.</p> <p>This distinction matters because latent planning relies on imagination. During each imagined rollout, the encoder provides the initial latent state, but the future is carried almost entirely by the predictor: each predicted latent becomes the input for the next prediction. If the predictor slightly underestimates motion, averages over possible futures, or drifts away from the correct physical trajectory, those errors can compound before the planner ever executes an action. Closed-loop MPC can reduce the damage by replanning from real observations, but it does not remove the need for the model to produce accurate imagined futures over the planning horizon.</p> <p>This is the regime I want to examine. LeWM demonstrates strong end-to-end training stability and meaningful frozen-encoder probes, but its evaluations stop short of the long-horizon predictor regime: frozen-encoder probes at horizon zero test the encoder, and short-horizon CEM planning reports aggregate task success. Neither shows how the predicted physical state and latent geometry evolve when the predictor is rolled forward open-loop for many steps.</p> <p>The deeper issue is that local <strong>one-step and long-horizon predictions are not the same objective</strong>. A deterministic predictor trained with MSE is encouraged to predict the <strong>conditional average</strong> next latent under its training distribution. When the encoded state is compressed, the action-conditioned dynamics are ambiguous, or the future contains contact-rich variation, this averaging can quietly suppress motion and erase persistent state. In pixel-space video prediction, this kind of averaging appears as blur. In latent dynamics, it may appear as something less visually obvious: slow latent velocity, overly straight trajectories, and physically wrong but statistically plausible futures.</p> <p>This concern is not specific to LeWM. Recent world-model work is increasingly shaped by the same pressure point: V-JEPA 2 (Assran et al., 2025) identifies long-horizon rollout degradation as a central limitation, HWM (2026, arXiv 2604.03208) proposes hierarchical planning to reduce long-horizon prediction burden, and LiveWorld (Duan et al., 2026, arXiv 2603.07145) introduces explicit memory and dynamic-entity machinery to continually evolve persistent states when entities leave view. These approaches differ architecturally, but they respond to a shared problem: <strong>stable representations alone do not guarantee persistent, action-relevant dynamics.</strong></p> <p>What is still missing is a concrete empirical characterization of how this failure appears inside a representative latent world model. Does the predicted latent collapse? Does it leave the manifold? Does it preserve global statistics while losing the correct physical future? Or is the one-step predictor itself already dynamically under-amplified? This blog studies those questions in <strong>LeWM, rolled out on Push-T</strong>.</p> <h2 id="what-lewm-is">What LeWM is</h2> <p>LeWM follows the Joint-Embedding Predictive Architecture (JEPA) recipe for world modeling. Instead of predicting future pixels, it learns a compact latent representation of each image observation and trains a predictor to model how that latent evolves under actions. This is what makes the approach attractive for planning: if the latent space captures the task-relevant physical state, a planner can search over imagined futures cheaply, without reconstructing images at every step. What distinguishes LeWM from earlier JEPA-style world models is that this setup is trained end-to-end from raw pixels on a single GPU, without stop-gradients, an EMA teacher, or a pretrained encoder.</p> <p>At a high level, LeWM has two learned components: an <strong>encoder</strong> and a <strong>predictor</strong>.</p> <p><strong>The encoder</strong> maps an image observation into a latent vector:</p> \[z_t = \text{enc}_\theta(o_t)\] <p>Here, \(o_t\) is the image observation at time \(t\), \(\text{enc}_\theta\) is the encoder with parameters \(\theta\), and \(z_t\) is the latent representation of that observation. In the released Push-T model, this latent is 192-dimensional. Intuitively, \(z_t\) compresses the physical scene — including the agent, block position, and block angle — into a vector useful for prediction and planning.</p> <p><strong>The predictor</strong> takes a short history of encoded latents together with the corresponding action input, and predicts the next latent. In simplified one-step notation:</p> \[\hat{z}_{t+1} = f_\phi(z_t, a_t)\] <p>Here, \(a_t\) is the action, \(f_\phi\) is the predictor with parameters \(\phi\), and \(\hat{z}_{t+1}\) is the predicted next latent. In the actual Push-T setup studied here, LeWM uses a 3-frame context window and frame-skip 5, so each predictor step corresponds to five raw environment steps.</p> <p>LeWM is trained with two losses. The first is the next-latent prediction loss:</p> \[L_{\text{pred}} = \lVert \hat{z}_{t+1} - z_{t+1} \rVert_2^2\] <p>Here, \(z_{t+1}\) is the encoder output on the actual next frame, and \(\hat{z}_{t+1}\) is the predictor’s estimate of that latent. The loss simply penalizes the squared distance between the predicted latent and the real next-frame latent. In words: the predictor is trained to land close to the encoder’s representation of what actually happened next.</p> <p>On its own, however, this prediction loss can lead to a trivial solution. The encoder could map every image to the same constant vector, making future prediction easy but useless. This is the standard JEPA representation-collapse problem. LeWM prevents this with <strong>SIGReg</strong>, a distributional regularizer that pushes the encoder’s latent outputs toward an isotropic Gaussian distribution. The practical effect is that the encoder cannot collapse all observations to a constant without paying a large regularization cost.</p> <p>One detail will matter later: SIGReg regularizes the encoder’s latent outputs during training, not the predictor’s open-loop outputs at inference time. The predictor is only indirectly shaped by SIGReg, through its MSE targets \(z_{t+1}\). So if predicted latents remain norm-stable during rollout, that stability is not explicitly enforced step by step; it is inherited indirectly from the learned dynamics.</p> <p>The key train-test distinction is the difference between one-step prediction and open-loop rollout. During training, the predictor receives real encoded latents as input:</p> \[\hat{z}_{t+1} = f_\phi(z_t, a_t)\] <p>But during open-loop rollout, the predictor is fed its own previous prediction:</p> \[\hat{z}_{t+1} = f_\phi(\hat{z}_t, a_t)\] <p>The symbol \(\hat{z}_t\) now refers to the model’s own predicted latent at time t, not the encoder’s latent from a real observation. This difference is small in notation but large in consequence. Once the model makes an error, the next prediction is conditioned on that error. Over many steps, small deviations can compound into a trajectory that remains statistically plausible but no longer tracks the correct physical future.</p> <p>LeWM uses this kind of latent rollout during planning. Starting from an encoded initial observation, the predictor imagines future latent states under candidate action sequences. A planner, such as CEM, can then score those imagined futures against a latent goal and choose actions accordingly. In closed-loop MPC, the agent may re-encode the real observation after executing some actions, but inside each planning rollout the future is still carried by the predictor. That is why predictor fidelity matters.</p> <p>The LeWM paper evaluates the model in several reasonable ways: frozen-encoder probes test whether real encoded latents contain physical quantities such as agent location, block location, and block angle; CEM planning reports aggregate task success; and decoded rollout visualizations show whether imagined trajectories look visually plausible. These evaluations support the paper’s main claims about stable end-to-end JEPA training and useful latent representations. However, they do not isolate the predictor’s horizon-by-horizon behavior: whether predicted latents preserve physical-state content and latent geometry when rolled forward open-loop over long horizons. That is the gap this blog studies.</p> <h2 id="experimental-setup">Experimental setup</h2> <p>The goal of this experiment is to test LeWM’s <strong>predictor</strong>, not merely its encoder. The encoder encodes useful physical information from real observations, but long-horizon planning depends on what happens after the model starts predicting future latents by itself. I therefore evaluate the released LeWM Push-T checkpoint under long-horizon open-loop rollout and ask whether the predicted latents continue to preserve task-relevant physical states as the rollout progresses.</p> <p>I use the released LeWM Push-T checkpoint without retraining and evaluate it on 1000 held-out Push-T episodes from <code class="language-plaintext highlighter-rouge">pusht_expert_train.h5</code>, the same dataset distribution used by the paper. The episodes are split into train, validation, and test partitions by episode index, and I inherit the exact split used to fit the frozen probes. Every rollout number reported here is therefore computed on test episodes that the probes never saw.</p> <p>The rollout is open-loop. For each episode, I encode the first three frames as context, then let the predictor run forward using its own previous predictions. At each predictor step, the model consumes its previous predicted latent, not a real encoded frame, together with the expert action sequence, and outputs the next predicted latent. Because the model uses frame-skip 5, each predictor step corresponds to five raw environment steps. I roll the predictor forward for 18 steps, producing predicted latents at raw-environment horizons from 15, the first predicted step after the three-frame context, to 100, the final evaluated horizon. Throughout this blog, “horizon” refers to this raw environment-step count.</p> <p>The physical-state probes follow the paper’s protocol. For each of three Push-T quantities — agent location, block location, and block angle — I train both a linear probe and an MLP probe on real frozen encoder latents. I primarily report the MLP results because they recover the physical quantities more accurately at horizon zero, but the linear probes are useful as a check on whether the same degradation pattern appears under a simpler decoder.</p> <p>To track how physical-state recoverability changes with rollout horizon, I report normalized MSE. For each quantity, the probe’s mean squared error is divided by the error of a constant predictor that always guesses the test-set mean. A normalized MSE of 1.0 therefore corresponds to the trivial baseline: ignoring the latent entirely and predicting the dataset average. Values below 1.0 mean the probe is extracting useful information from the latent. Values above 1.0 mean that, under this probe, the predicted latent is less informative than the constant-mean baseline for that physical quantity. This threshold is the reference point for the degradation curves that follow.</p> <p>As a sanity check, these probes reproduce the paper’s Table 1 accuracy on real encoded latents before rollout:</p> <table> <thead> <tr> <th>Physical quantity</th> <th>Probe</th> <th>This work (MSE)</th> <th>Paper (MSE)</th> </tr> </thead> <tbody> <tr> <td>Agent location</td> <td>Linear</td> <td>0.043</td> <td>0.052</td> </tr> <tr> <td>Agent location</td> <td>MLP</td> <td>0.005</td> <td>0.004</td> </tr> <tr> <td>Block location</td> <td>Linear</td> <td>0.021</td> <td>0.029</td> </tr> <tr> <td>Block location</td> <td>MLP</td> <td>0.001</td> <td>0.001</td> </tr> <tr> <td>Block angle</td> <td>Linear</td> <td>0.175</td> <td>0.187</td> </tr> <tr> <td>Block angle</td> <td>MLP</td> <td>0.027</td> <td>0.021</td> </tr> </tbody> </table> <p>These horizon-zero values are far below the normalized-MSE failure threshold, confirming that the probes recover physical state when their inputs are real encoder latents. The question is what happens when those same probes are applied to latents produced by the predictor during open-loop rollout. That is the puzzle of the next section.</p> <h2 id="the-puzzle-a-three-tier-degradation-pattern">The puzzle: a three-tier degradation pattern</h2> <p>When the probes trained on real encoded latents are applied to the predictor’s open-loop predictions, the degradation is not uniform. If the predictor were simply accumulating generic noise, I would expect all decoded physical quantities to deteriorate in roughly the same way. If the latent had collapsed completely, I would expect all probes to fail together. What comes out is not one degradation curve but three, each with a distinct temporal profile. That is the puzzle of this section.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/probe_normalized_mse_by_horizon-480.webp 480w,/assets/img/lewm/probe_normalized_mse_by_horizon-800.webp 800w,/assets/img/lewm/probe_normalized_mse_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/probe_normalized_mse_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Figure: Normalized MSE vs rollout horizon. Each curve shows a frozen linear or MLP probe applied to predicted rollout latents. The reference line at normalized MSE = 1.0 is the constant-mean baseline. Values above 1.0 mean the probe is doing worse than always predicting the test-set average for that quantity.</figcaption> </figure> <p><strong>Tier 1: block location degrades smoothly and slowly.</strong> At horizon 15 the MLP probe recovers block location almost perfectly (normalized MSE around 0.005). The error then climbs steadily, crosses the 1.0 threshold around horizon 65, and reaches 1.55 by horizon 100. This smooth, sigmoidal shape — no sharp transitions — is the classic signature of compounding error: each predictor step introduces a small displacement, the displacements accumulate, and block position stays partially recoverable for a while before drifting past the constant-mean baseline. As with the other two quantities, though, the probe curve alone cannot confirm that compounding is the actual mechanism; what it does show is that block location is the slowest and smoothest of the three to degrade.</p> <p><strong>Tier 2: agent location fails early, then plateaus.</strong> At horizon 15 the MLP normalized MSE is already 0.20 — substantially worse than horizon zero — and by horizon 30 it has reached about 1.0. After that, it goes almost nowhere: from horizon 30 through horizon 100 the curve sits between roughly 1.0 and 1.2 and barely moves. This shape is not the same as smooth block-location drift. It suggests early loss of agent-position information: after a few predictor steps, the predicted latent no longer preserves enough of it for the frozen probe to recover a useful location estimate. From the probe curve alone, however, I should not yet call this a literal fixed point. It could reflect fixed-point-like convergence, conditional-mean smoothing, weak action sensitivity, or some combination of these.</p> <p><strong>Tier 3: block angle survives briefly, then breaks sharply.</strong> Up to horizon 30 the MLP probe recovers block angle well (normalized MSE around 0.38, well below the threshold). Between horizon 30 and horizon 35 the curve more than doubles, reaching 0.89 — essentially the threshold. It then crosses 1.0 between horizon 35 and horizon 40, and continues climbing to settle at the highest error of all three quantities — roughly 2.35 by horizon 100. This is not a smooth curve, it has a sharp jump. A tempting interpretation is that rotational state is represented more fragilely than translational block position. This is consistent with block angle being harder to recover even from real encoded latents at horizon zero, but the probe curve alone does not prove a specific capacity-allocation mechanism. What it does show is that angle information is much less robust under predictor rollout.</p> <p>Taken literally, these three shapes invite three different explanations. Block location looks like gradual compounding drift. Agent location looks like early saturation or defaulting. Block angle looks like a fragile variable that survives briefly and then fails.</p> <p>Pearson correlation <a id="ref-probe_pearson_r_by_horizon"></a><a class="jumpref" href="#fig-probe_pearson_r_by_horizon">(see figure)</a> between predicted and true targets tracks the same ordering across horizons as normalized MSE, with one nuance: even where normalized MSE exceeds the constant-mean baseline, correlations do not collapse to zero — at horizon 100, MLP block location still shows r ≈ 0.35 and agent location r ≈ 0.21. This means the predicted latents are not pure noise. They still carry some coarse state information, but the frozen probe can no longer convert that residual ordering into accurate physical predictions under the MSE metric.</p> <p>But one more observation does not fit cleanly into this three-mechanism story. At horizon zero the MLP probes are substantially better than the linear probes for all three quantities — the linear-versus-MLP gap that the LeWM paper documents in Table 1, and that signals non-linearly accessible structure in the latent. Under rollout, this gap shrinks. For block location the linear and MLP curves are essentially overlapping from horizon 35 onward; for block angle they converge until horizon 50 and then diverge with a gap; only agent location holds a small gap throughout. The rollout is therefore not merely damaging one particular readout of physical state. It is eroding the richer latent structure that made non-linear probing useful in the first place. Whatever is going wrong is wiping out both kinds of accessible information at the same time, and on a similar schedule. That is hard to reconcile with three independent failure modes operating on three independent pieces of the latent.</p> <p>A methodological aside is also worth flagging. The LeWM paper’s Figure 7 shows decoded latent rollouts at horizon 35 that look visually plausible. The quantitative picture at the same horizon is starker: block location MLP normalized MSE is around 0.23 (well below threshold), block angle is at 0.89 (effectively at the threshold), and agent location MLP is at roughly 1.05 (just past it). In other words, a decoded frame can look reasonable even when task-relevant physical quantities have already degraded substantially. This is not an objection to the paper’s figure — qualitative rollouts are useful — but it is the kind of mismatch that a 1000-episode probe-based evaluation surfaces and a few decoded frames cannot.</p> <p>The physical probes reveal the symptom: three physical quantities degrade in three distinct ways. The latent-geometry diagnostics ask what kind of predicted trajectory could produce that symptom. That is why the next step is to inspect the rollout latents directly.</p> <h2 id="the-geometric-picture-slow-straight-adrift">The geometric picture: slow, straight, adrift</h2> <p>The probe results in the previous section tell us <em>what</em> is lost — three physical quantities in three different ways — but not what kind of latent trajectory produces that loss. To answer that, I set the probes aside and measure the predicted latents directly against six geometric diagnostics. The charts are included in the appendix and footnoted below.</p> <p>I start with what is preserved, because it is the part that makes the failure easy to miss.</p> <p><strong>The latent stays the right size.</strong> A natural first worry is that the predicted latent might blow up, shrink to zero, or collapse into a small region during autoregressive rollout. It does not. The encoder’s latents have a tightly concentrated norm: across 2.1 million encoded frames, the mean L2 norm is 13.89 with a standard deviation of 0.82. <a id="ref-norm_pred_vs_true_by_horizon"></a><a class="jumpref" href="#fig-norm_pred_vs_true_by_horizon">(see figure)</a> The predicted latents stay inside this shell throughout rollout. The predicted norm starts slightly high at 14.29 at horizon 15, drifts down to about 13.62 by horizon 60, and remains there; its z-score against the encoder reference never leaves the range −0.33 to +0.49. In magnitude terms, a predicted latent at horizon 100 is indistinguishable from a normal encoder output.</p> <p>This norm stability is consistent with LeWM’s SIGReg-shaped latent space, but it does not establish that SIGReg alone is responsible. SIGReg regularizes encoder outputs during training; predictor outputs inherit that geometry only indirectly through the MSE target latents.</p> <p>That is the surprising part: by this one global measure, the rollout looks healthy. The next five diagnostics show that it is not.</p> <p><strong>The latent is in the wrong place.</strong> The most direct measure is the squared distance between the predicted latent and the true encoded latent of the same future frame, averaged per dimension. <a id="ref-latent_per_dim_mse_by_horizon"></a><a class="jumpref" href="#fig-latent_per_dim_mse_by_horizon">(see figure)</a> This grows from 0.062 at horizon 15 to 1.92 at horizon 100, saturating around horizon 60–70. The saturation value is the informative part. The encoder latents have roughly unit variance per dimension. One quick sanity check is the norm: the latent dimension is 192, so if each dimension has variance about 1.0, a typical latent vector should have L2 norm around \(\sqrt{192} \approx 13.86\). The measured mean norm is 13.89, which is almost exactly that value. This matters because two unrelated unit-variance latents are expected to have a squared difference of about 2.0 per dimension: each latent contributes about 1.0 variance, and the two variances add. So when the rollout’s per-dimension MSE to the true future approaches 1.92, the predicted latent is almost exactly as far from the true latent as a <strong><em>randomly chosen</em></strong> latent would be, not just a small local prediction error. It may still look plausible in broad distributional terms, but it has largely lost the information identifying this episode’s specific future.</p> <p><strong>It drifts from the empirical manifold, but does not leave the distribution entirely.</strong> A latent can be far from its specific target yet still be close to some other real encoded state. To test this, I measure the distance from each predicted latent to its nearest neighbor among real train/validation encoded latents, and compare it to the same nearest-neighbor distance for the true future latent. Predicted latents are consistently farther from the empirical encoded manifold than true future latents, by a factor of about 4.5 to 11 across horizons. The ratio peaks near 11× around horizon 25 and falls to about 4.5× by horizon 100. <a id="ref-manifold_drift_ratio"></a><a class="jumpref" href="#fig-manifold_drift_ratio">(see figure)</a> This does not mean the predicted latent is returning to the manifold; the absolute nearest-neighbor distance continues to grow <a id="ref-manifold_min_sq_l2"></a><a class="jumpref" href="#fig-manifold_min_sq_l2">(see figure)</a>. Rather, later true future latents also become farther from the finite reference set, likely because later-frame states occupy sparser regions of the sampled dataset.</p> <p>The ratio is therefore real but needs careful interpretation. In absolute terms, the predicted latent is still much closer to some real encoded latent than to the correct future latent. For example, at horizon 100, its nearest-manifold per-dimension MSE is about 0.09 <a id="ref-manifold_min_per_dim_mse"></a><a class="jumpref" href="#fig-manifold_min_per_dim_mse">(see figure)</a>, while its per-dimension MSE to the correct future latent is about 1.92 <a id="ref-latent_per_dim_mse_by_horizon-2"></a><a class="jumpref" href="#fig-latent_per_dim_mse_by_horizon">(see figure)</a> — roughly a 20× gap. So the predicted latent is not flying into arbitrary empty space; it remains near the broad encoded distribution, but not near the correct future state. The right description is not off-manifold collapse. It is a wrong-but-plausible latent: statistically latent-like, but no longer anchored to the specific physical state it should represent.</p> <p>Two trajectory diagnostics explain how the rollout gets there.</p> <p><strong>It moves too slowly.</strong> I measure step-to-step velocity as the norm of the difference between consecutive latents. The true encoded trajectory moves at roughly 4 to 6 latent units per step, increasing over the episode as the dynamics become more active. The predicted trajectory moves at roughly 1.0 latent unit per step, nearly flat across rollout. The predicted latent is therefore moving about four to six times too slowly. This is the clearest geometric signature: the predictor is not frozen, but <strong>it systematically under-amplifies the magnitude of real latent motion.</strong></p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/latent_velocity_norm_by_horizon-480.webp 480w,/assets/img/lewm/latent_velocity_norm_by_horizon-800.webp 800w,/assets/img/lewm/latent_velocity_norm_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/latent_velocity_norm_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Figure: Step-to-step velocity norm of predicted versus true encoded latents by horizon. The true latent moves several times faster than the predicted latent at every horizon, and the gap does not close.</figcaption> </figure> <p><strong>It also moves too straight.</strong> Following the temporal-straightness measure LeWM studies during training — the mean cosine similarity between consecutive velocity vectors, where 1.0 means a perfectly straight path and 0 means uncorrelated turns — I measure straightness during rollout. <a id="ref-straightness_pred_vs_true_by_horizon"></a><a class="jumpref" href="#fig-straightness_pred_vs_true_by_horizon">(see figure)</a> The predicted trajectory is straighter than the real encoded trajectory at every horizon: the predicted mean cosine is about 0.70, while the true mean is about 0.53. The histogram makes the contrast sharper. <a id="ref-straightness_cosine_histogram"></a><a class="jumpref" href="#fig-straightness_cosine_histogram">(see figure)</a> Predicted velocities pile up near cosine 1.0, while true velocities spread more broadly around 0.5 to 0.8. The real latent trajectory curves as the physical state evolves; the predicted trajectory glides along a slower, more collinear path.</p> <p>This matters because straightness can be misleading. In the LeWM paper, latent path straightening is treated as a useful emergent representational property during training. But during rollout, straightness is only useful if it tracks the correct future. Here it appears together with low velocity and high target MSE. In that context, excessive straightness is not evidence of better dynamics; it is evidence of over-smoothed dynamics.</p> <p>Finally, I diagnose what kind of error this is.</p> <p><strong>The error is mostly episode-specific, not one shared drift direction.</strong> A simple explanation would be that every rollout drifts toward the same average Push-T state. The systematic-bias diagnostic rules this out as the main cause. I decompose total error into a systematic component — the mean error vector shared across episodes — and a per-episode residual component. The systematic component is small: it explains only about 2–5% of total error energy across horizons. <a id="ref-bias_fraction_by_horizon"></a><a class="jumpref" href="#fig-bias_fraction_by_horizon">(see figure)</a> The shared bias component is tiny relative to total error at every horizon. <a id="ref-bias_vs_total_per_dim_error"></a><a class="jumpref" href="#fig-bias_vs_total_per_dim_error">(see figure)</a> The remaining error is overwhelmingly episode-specific. The predictor is therefore not collapsing every rollout onto one shared destination. What the episodes share is the pattern of failure: slow, overly straight, and adrift from the empirical manifold.</p> <p>Put together, the geometric picture is coherent. The predicted latent keeps the right magnitude and remains statistically well-formed, but it travels too slowly, too straight, and moderately away from the empirical manifold of real latents. Meanwhile, its MSE to the correct future latent rises to the scale of unrelated latents. The failure mode is not “the latent explodes” or “the latent collapses.” It is subtler: <strong>the predictor produces stable-looking latents that follow a slow, over-straight, wrong trajectory</strong>.</p> <p>A trajectory that is systematically too short and too straight is a natural geometric signature of <strong>averaging</strong>. If each one-step prediction is pulled toward the conditional average over possible next latents, the predicted step will be shorter than a typical real step, smoother than a typical real path, and more likely to land in the gaps between real trajectories. This would naturally produce a latent that remains distributionally recognizable while drifting away from the correct future.</p> <p>But this is still a hypothesis. Everything so far has been measured during open-loop rollout, where the predictor feeds on its own outputs. The slow, straight trajectory could be intrinsic to the <strong>predictor’s one-step map</strong>, or it could appear only after the predictor is <strong>fed its own imperfect predictions and errors compound</strong>. The next experiment isolates these possibilities with a teacher-forced control.</p> <h2 id="the-control-under-amplification-is-intrinsic-not-a-compounding-artifact">The control: under-amplification is intrinsic, not a compounding artifact</h2> <p>The previous section left one question open. The predicted open-loop trajectory is too slow and too straight — but is that a property of the predictor’s one-step map, or does it only appear once the predictor is fed its own imperfect predictions and the errors pile up? These are very different diagnoses. If the slowness is intrinsic to the one-step map, re-grounding during planning cannot repair it — each freshly predicted step is under-amplified again from a clean input. Frequent re-grounding can bound how far errors compound, but it cannot make the individual steps the right length and direction. If it is purely a compounding artifact, the one-step map is fine and the problem is mainly the feedback loop.</p> <p>To test this, I run a teacher-forced control. Instead of letting the predictor feed on its own previous latents, I feed it <strong>real encoded context latents</strong> from the actual trajectory at every horizon and ask it to predict only the next step. In other words, the input to the predictor stays on the encoder’s real trajectory. This removes the open-loop feedback loop and accumulated error. If under-amplification were mainly caused by compounding off-manifold feedback, teacher forcing should restore realistic one-step motion.</p> <p><strong>It does not.</strong></p> <p>The teacher-forced predicted one-step velocity (the blue curve) stays almost flat around <strong>33%</strong> of the true encoded latent velocity from horizon 15 through 100. Even when the predictor is handed real encoded context at every step, the step it takes is only about one-third the length of the true step. The velocity under-shoot is not created by feedback; it is already present in the learned one-step map.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio-480.webp 480w,/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio-800.webp 800w,/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Figure: Predicted/true velocity ratio under teacher forcing (flat near 0.33) versus open-loop rollout (decaying to about 0.16), by horizon. The dashed line at 1.0 marks a faithful predictor. Both curves sit far below it — and far below the upper reference line where a predictor that merely added isotropic noise would land, which would push the ratio above 1.0. The predictor is damping real motion, not injecting noise.</figcaption> </figure> <p>The open-loop curve (orange) tells the other half of the story. It starts at the same 0.33 ratio at horizon 15, then drops to about 0.16 by horizon 40 and stays near there for the rest of the rollout. Feeding the predictor its own already-under-amplified latents makes later steps even shorter. In that sense, feedback does not create the one-step damping, but it makes the rollout worse by compounding it.</p> <p>The error magnitudes confirm the same split from a different angle. Under teacher forcing, the per-dimension one-step error stays relatively small and nearly flat, growing only from about 0.06 to 0.14 across the rollout. <a id="ref-tf_one_step_error_by_horizon"></a><a class="jumpref" href="#fig-tf_one_step_error_by_horizon">(see figure)</a> In open loop, the per-dimension error grows to about 1.92 by horizon 100 — roughly fourteen times larger. So feedback still matters. It determines how far the predicted trajectory eventually lands from the true future. But it does not explain why each local step is too short. The local under-shoot is present before the feedback loop begins; open-loop rollout compounds an already under-amplified map.</p> <p>This distinction matters for diagnosis. If the failure came only from open-loop feedback, then manifold correction, shorter rollouts, or more frequent re-encoding could address it. But the teacher-forced result shows that the local transition itself is already damped: even on real encoded inputs, the predictor systematically predicts less latent motion than the world actually contains.</p> <p>This pattern is consistent with a conditional-mean smoothing interpretation. Under an MSE objective, the population-optimal deterministic predictor is the conditional mean of the possible next latents: the single point that minimizes squared error across plausible futures. If multiple next latents are compatible with the same encoded context and action, their average can have a shorter displacement than a typical realized step, because components in different directions cancel. More precisely, the average squared length of a true step decomposes exactly into the squared length of the conditional-mean step plus the variance of the possible futures around that mean. So the wider the future distribution, the smaller the share of that budget the mean step can keep — a deterministic mean prediction gives up motion in proportion to how much the futures diverge in directions.</p> <p>But this is where the certainty should stop. The teacher-forced control proves that the velocity under-shoot lives in the one-step map rather than being created by open-loop feedback. It does not, by itself, prove why the one-step map is under-amplified. Conditional-mean smoothing is the best current interpretation of the geometry, but several causes could make the effective future distribution wide or make the learned map contractive: missing dynamical state in the latent, weak action conditioning, contact ambiguity, finite predictor capacity, optimization bias, or genuine stochasticity in the transition. The velocity ratio alone cannot distinguish these. I return to these alternatives in the implications and limitations sections.</p> <h3 id="not-just-lag-along-track-vs-perpendicular-error">Not just lag: along-track vs perpendicular error</h3> <p>Velocity magnitude tells us the predicted step is too short. But it does not tell us whether the step points in the right direction. A predictor could be slow but still directionally correct: it might move one-third of the way toward the true next latent, like a lagged version of the real dynamics. That would be a more forgiving failure — still wrong, but at least moving along the right path, the kind of thing a gain correction or re-grounding might recover.</p> <p>The along-track / perpendicular decomposition tests this.</p> <p>For each real transition, I take the true latent step as the reference direction: where the encoded trajectory actually moved from the current frame to the next frame. Then I decompose the predicted step into two parts. The <strong>along-track</strong> component is the part of the predicted step that moves forward in the true direction. The <strong>perpendicular</strong> component is the part that moves sideways, orthogonal to the true direction.</p> <p>The magnitude numbers reveal this. At a representative mid-rollout horizon, the true step has length about 5.7 and the teacher-forced predicted step has length about 1.85. If the predicted step were perfectly aimed but too short, the smallest possible error would be the difference between those lengths: about \(5.7 - 1.85 \approx 3.8\) But the measured one-step error is about 4.3, larger than this pure-lag lower bound. A short but correctly aimed arrow cannot produce an error larger than the distance it failed to cover along the true direction. The extra error means the predicted arrow must be tilted away from the true direction. Working the same numbers through into the two directions: of the true step’s length, only about a quarter shows up as forward progress along the true direction, and a comparable amount goes sideways. So the predictor recovers roughly one-quarter of the real motion in the right direction and spends a similar share drifting off it.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/along_track_perpendicular_decomposition-480.webp 480w,/assets/img/lewm/along_track_perpendicular_decomposition-800.webp 800w,/assets/img/lewm/along_track_perpendicular_decomposition-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/along_track_perpendicular_decomposition.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Figure: Teacher-forced one-step decomposition. At a representative horizon, the true latent step is the motion the predictor should reproduce. The teacher-forced predicted step is much shorter and also tilted away from the true direction. Decomposing it into an along-track component and a perpendicular component shows that the predictor makes only limited forward progress while spending a comparable amount of motion sideways.</figcaption> </figure> <p>This is the intuitive meaning of the perpendicular component. The predicted step is not merely a scaled-down version of the true step. It has limited forward progress along the true latent transition and a substantial sideways component. In road terms, the predictor does not just move too slowly down the road; it also drifts off the road.</p> <p>There is one caveat. The simple 5.7 / 1.85 / 4.3 argument uses average step lengths and average error at a horizon, not a per-episode projection of every predicted step onto its own true direction. That means the exact fractions — such as “one-quarter forward progress” or “comparable sideways drift” — should be treated as approximate. But the qualitative conclusion is robust to that shortcut: across horizons, the measured error exceeds the pure-lag lower bound, which would not happen if the predicted steps were simply shorter but correctly aligned. A per-episode projection would be the cleaner way to report the exact along-track and perpendicular fractions.</p> <p>This matters because it sharpens the mechanism. The one-step map is not only under-amplified but also partly misdirected, even based on real encoded latents and actions. Repeating that step many times naturally produces the geometry observed earlier: a trajectory that remains smooth and latent-like, but becomes slow, and displaced from the correct future.</p> <p>With under-amplification confirmed as a one-step property, and the step shown to be misdirected rather than merely lagging, we can now ask why a single local failure mode produces the three different physical signatures from the puzzle.</p> <h2 id="synthesis-one-mechanism-three-physical-signatures">Synthesis: one mechanism, three physical signatures.</h2> <p>At the start, the probe curves for the three physical quantities looked like three separate failures. Block location drifted gradually. Agent location failed early and then plateaued. Block angle survived briefly and then broke sharply. Taken in isolation, those curves invite three different explanations: compounding position error, agent-state collapse, and fragile angle representation.</p> <p>The geometric diagnostics point to a cleaner interpretation. The predicted latents are not collapsing in the usual sense: their norms stay inside the encoder reference shell, and they are not dominated by one global bias direction. But they lose target-specific alignment — their MSE to the true future latent rises toward the scale of unrelated latents — while drifting farther from the empirical manifold than true futures, moving far too slowly, and following trajectories that are too straight. The teacher-forced control localizes the source: even fed real encoded context at every horizon, the predictor’s one-step velocity is only about one-third of the true velocity. The along-track / perpendicular decomposition sharpens it further — the predicted step is not merely shorter than the true step; it makes too little forward progress and also drifts sideways.</p> <p>So the simplest synthesis is not three independent mechanisms. It is one local failure mode with three physical signatures.</p> <p>That failure mode is a smoothed, under-amplified transition map: given a real state and action, the predictor’s next latent is distributionally plausible but moves too little and slightly off-direction — and repeating it produces the rollout geometry above. This is consistent with conditional-mean smoothing, though the underlying cause remains open. What makes it a useful synthesis is that this single local defect, filtered through three physical quantities with different sensitivities, accounts for all three degradation curves:</p> <p><strong>Block location degrades gradually because coarse object position is comparatively robust.</strong> The block is a large, visually salient object, and its position changes smoothly relative to the rollout horizon. Even if each predicted latent step is too short, some coarse block-position information can survive for many steps. The result is not immediate failure, but smooth drift: the predicted block remains recognizable for a while, then slowly separates from the true future. This matches the block-location curve: low early error, steady degradation, and a late crossing of the constant-mean baseline.</p> <p><strong>Agent location fails early because it is highly action-dependent.</strong> The agent position is the part of the state most directly tied to local action updates. If the predictor under-amplifies motion, the agent’s predicted position quickly becomes stale, averaged, or default-like. Unlike the block, whose coarse position can remain plausible under slow drift, the agent must move accurately at each step for its location to stay recoverable. Once those local updates are too small, the probe rapidly loses useful agent-position information. That explains the early jump and plateau: after a few predictor steps, the latent no longer contains an accurately updated agent state, so additional horizon does not change the error shape much. Because this is the tier where the action matters most, it is also the cleanest place to probe further — a rollout driven by null or random actions would show whether the predictor is also under-using the action it is given, on top of the smoothing. I take that up in the next section.</p> <p><strong>Block angle breaks sharply because rotational state is fragile under smoothed dynamics.</strong> Angle is not just another coordinate. It depends on contact geometry, local torque, and small changes in how the agent pushes the block. A smoothed latent transition can preserve rough object position while failing to capture the rotational consequence of contact. This also fits the probe sanity check: block angle is harder to recover than block location even from real encoded latents, suggesting that rotational state is less robustly represented or less easily recoverable than translational block position. Under rollout, a few under-amplified or slightly misdirected steps may still leave angle below the failure threshold, but once the rotational state slips, the error rises sharply. That gives the observed cliff-like pattern around the mid rollout.</p> <p>The following figure shows the horizon-35 physical reconstructions from MLP-probe-decoded predicted latents, from the best 12 of the 1000 test episodes. Even in these favorable examples, the block pose can remain visually plausible while the agent is visibly displaced. This makes the three-tier pattern concrete: coarse block state survives longer, agent state is already unreliable.</p> <details> <summary>Show figure</summary> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/horizon35_physical_recons-480.webp 480w,/assets/img/lewm/horizon35_physical_recons-800.webp 800w,/assets/img/lewm/horizon35_physical_recons-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/horizon35_physical_recons.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> </details> <p>This figure is useful because it shows why qualitative rollout inspection can be misleading. A decoded future frame can look plausible if the block remains roughly in the right place. But planning depends on the full task state, not only whether the image looks reasonable. At horizon 35, the probe curves already show a split: block location is still relatively accurate, agent location is near or beyond the constant-mean baseline, and block angle is close to failure. The reconstruction makes that split visible. The model has not lost all structure, but it has lost the right structure for control.</p> <p>The important point is that “stable latent” and “faithful dynamics” are different properties. SIGReg-style regularization can keep the latent distribution well-shaped: the predicted vectors can have normal norms and remain broadly latent-like. But local dynamical fidelity asks for more. The predicted latent must move by the right amount, in the right direction, under the right action, while preserving the physical variables that matter for planning. LeWM’s rollout preserves the global shell better than it preserves that local state update.</p> <p>This is why the earlier three-tier degradation should not be read as three unrelated bugs. It is more naturally read as one smoothed latent dynamics problem, filtered through the different demands of each physical variable. Block position tolerates smoothing for a while. Agent position does not. Block angle fails when contact-sensitive rotational information is no longer carried accurately enough. The same under-amplified one-step map produces all three signatures.</p> <p>The conclusion is therefore narrower, but stronger, than simply saying “LeWM rollouts degrade.” In this Push-T checkpoint, long-horizon degradation is not driven by classical norm collapse or one shared global drift vector. It is driven by a predictor that produces stable-looking but dynamically under-amplified latents: slow, too straight, partly sideways, and increasingly detached from the correct future. The next question is what this implies for latent world models more broadly.</p> <h2 id="limitations-and-future-work">Limitations and future work</h2> <h3 id="limitations"><strong>Limitations</strong></h3> <p>This study is a mechanistic characterization of one LeWM Push-T checkpoint, not a final verdict on latent world models. The main finding is clear: in long-horizon rollout, LeWM’s predicted latents remain globally well-formed but become slow, overly straight, partly misdirected, and increasingly detached from the correct future. The limitations are about how far that finding can be generalized, and what causal explanation it supports.</p> <p>The most important limitation is that the root cause of the one-step under-amplification is not yet established. The teacher-forced control shows that the velocity under-shoot is intrinsic to the learned one-step map, not merely caused by open-loop feedback. But it does not tell us why the one-step map is under-amplified. Several explanations remain plausible: the environment may be genuinely stochastic; the latent may discard dynamical state needed for prediction; the predictor may under-use the action input; contact dynamics may make the next state ambiguous; the predictor may be too small or insufficiently optimized.</p> <p>The second limitation is scope. This blog studies a single released checkpoint on a single environment, Push-T. Push-T is useful because it has interpretable physical variables and contact-rich manipulation, but it is still one task. The observed failure could be specific to Push-T, to LeWM’s predictor, or to the released checkpoint. A stronger claim would require repeating the same diagnostic suite across multiple environments — for example grasping, pushing, and deformable-object tasks — and across multiple latent world model families.</p> <p>The third limitation is probe-based measurement. The physical-state curves measure what frozen probes trained on real encoder latents can recover from predicted latents. This is the right test if the question is whether rollout latents remain compatible with the encoder’s learned physical representation. But it is still indirect. Predicted latents may shift away from the distribution the probes were trained on, so probe failure could partly reflect readout mismatch rather than absolute loss of information. This is an intrinsic difficulty with latent world models: when the rollout lives in a hidden representation, degradation is not directly visible unless we build diagnostics around it.</p> <p>This is also why the failure mode is easy to miss. In a pixel or explicit-state world model, a degraded rollout is often visible: objects blur, teleport, disappear, or violate geometry. In a latent world model, the predicted vector can keep a normal norm and remain statistically well-behaved while silently losing task-relevant state. That is exactly what happens here. The horizon-35 reconstruction makes the point concrete: the block can remain roughly plausible while the agent is visibly displaced. A latent model gives fewer warning signs than a pixel model, which makes latent-space characterization more important, not less.</p> <p>Two narrower caveats round this out. The along-track / perpendicular split comes from a calculation on average step lengths, not a per-episode projection, so the exact “one-quarter forward, comparable sideways” figures are representative rather than precise — though the qualitative claim, that a real sideways component exists, holds at every horizon, since the measured error exceeds the pure-lag lower bound throughout. And open-loop rollout is not closed-loop planning: I measure the model imagining forward on its own, not a planner replanning from real observations each step. Closed-loop replanning would mask some of this degradation. But the planner’s action quality still depends on the fidelity of the open-loop rollout that justifies each plan, so open-loop accuracy is the right thing to isolate first.</p> <h3 id="future-work">Future work</h3> <p>First, test action sensitivity with true-action, null-action, and random-action rollouts. If the predicted velocity and physical-state degradation barely change when the action sequence is removed or randomized, then the predictor is not using action strongly enough. That would explain why agent location fails early: agent position is highly action-dependent, so a weakly action-conditioned predictor would average over possible agent futures. If true actions produce meaningfully better velocity and physical preservation than null or random actions, then the failure is not simply action neglect.</p> <p>Second, probe the latent for velocity. The Table 1 probes show position is recoverable from a real latent; they do not show whether velocity is. But predicting forward needs motion, not just position — knowing where the block is tells you nothing about where it is going. If velocity turns out to be poorly recoverable, then the predictor literally cannot see which way things are moving, and the wide distribution of futures it averages over is an information problem rather than a world-randomness one. This speaks straight to the central question of whether the latent carries persistent, action-relevant state.</p> <p>Third, estimate local conditional variance directly. For a given encoded context and action, find nearby examples in the dataset with similar context latents and similar actions, then measure the spread of the next latents that actually followed. If that local next-latent distribution is wide, and the predictor’s output lies near its empirical mean, then the conditional-mean smoothing interpretation becomes much stronger. If the local distribution is narrow but the predictor still under-shoots, then the failure is more likely due to capacity, optimization, or architecture-induced contraction.</p> <p>Fourth, repeat the diagnostic suite across more models and environments. The same analysis should be applied to other LeWM checkpoints, other manipulation tasks, and other latent world models. V-JEPA 2-AC is a natural comparison point because it adapts a large self-supervised video model into an action-conditioned latent world model for robot planning.</p> <h3 id="where-this-sits-in-current-work">Where this sits in current work</h3> <p>The field is already building fixes for this degradation, from several directions. V-JEPA 2’s action-conditioned predictor adds a rollout loss on top of teacher forcing, training the model on its own multi-step predictions rather than only single steps — directly targeting the train/test mismatch that LeWM’s one-step MSE leaves untouched. Hierarchical planning with latent world models reduces the number of error-prone steps by reasoning at a coarser temporal scale. LiveWorld, in the video-generation setting, adds explicit persistent-state machinery so that entities are not frozen when they leave view. These differ in what they touch — the training signal, the planning hierarchy, the architecture — but they share a starting assumption that the rollout degrades or become frozen. What has been missing is a mechanical account of <em>how</em> it degrades inside a latent world model. That account is what this work aims to add, and it is what would tell you which of these fixes is addressing the actual cause rather than the symptom.</p> <h3 id="next-step">Next Step</h3> <p>The broader next step is therefore not just to ask whether rollouts look plausible. It is to ask whether a world model preserves the state needed for counterfactual reasoning, intervention planning, and robust long-horizon foresight. This blog shows one concrete failure mode: stable-looking latents can lose the physical future they are supposed to represent. The next experiments should determine why the distribution of futures the predictor averages over is so wide — whether it reflects weak action conditioning, missing dynamical state, limited predictor capacity, or genuine uncertainty in the task itself.</p> <h2 id="reproducibility-and-references">Reproducibility and references</h2> <p>All experiments use the released LeWM Push-T checkpoint without retraining or fine-tuning. Evaluation runs on 1000 held-out Push-T episodes from <code class="language-plaintext highlighter-rouge">pusht_expert_train.h5</code>, using the same train/validation/test split by episode index used to fit the frozen probes, so every rollout number is computed on test episodes the probes never saw. Each rollout encodes a 3-frame context and rolls the predictor forward 18 steps at frame-skip 5, covering raw environment horizons 15 to 100. Seed 42 throughout.</p> <p>The physical-state probes follow the LeWM protocol: linear and MLP probes trained on real frozen encoder latents to decode agent location, block location, and block angle, then applied frozen to predicted rollout latents. Normalized MSE divides each probe’s error by that of a constant predictor guessing the test-set mean. The geometric diagnostics evaluate predicted latent trajectories directly: per-dimension MSE to the true future latent; nearest-neighbor distance to an empirical train/validation encoded-latent reference set; norm trajectory against the encoder reference shell (mean 13.89, std 0.82, n = 2,105,102); step-to-step velocity norm; temporal straightness (mean cosine between consecutive velocity vectors); systematic-bias decomposition; and teacher-forced one-step controls. The along-track / perpendicular split is computed by law of cosines on mean magnitudes per horizon and is approximate pending a per-episode projection.</p> <p>Every numerical claim in this post is drawn from the JSON report files listed below, not read from plots. The figures shown inline are a selective subset; the full diagnostic set and reports are in the repository.</p> <p>This appendix shows the full diagnostic set, grouped into collapsible sections — the page stays short by default, so expand only the ones you want. The underlying JSON/Markdown reports — the source of every numerical claim — are in the accompanying repository.</p> <details open=""> <summary>Probe diagnostics</summary> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/probe_normalized_mse_by_horizon-480.webp 480w,/assets/img/lewm/probe_normalized_mse_by_horizon-800.webp 800w,/assets/img/lewm/probe_normalized_mse_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/probe_normalized_mse_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Probe normalized MSE vs rollout horizon. Frozen linear and MLP probes for agent location, block location, and block angle applied to predicted rollout latents; the reference line at 1.0 is the constant-mean baseline. (Also shown in the main text.)</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/probe_raw_rmse_by_horizon-480.webp 480w,/assets/img/lewm/probe_raw_rmse_by_horizon-800.webp 800w,/assets/img/lewm/probe_raw_rmse_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/probe_raw_rmse_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Probe raw (un-normalized) RMSE vs rollout horizon for the same six probes. Agent-location error saturates early and high, block-location error climbs steadily, and block-angle error stays small in absolute pixel units.</figcaption> </figure> <div id="fig-probe_pearson_r_by_horizon"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/probe_pearson_r_by_horizon-480.webp 480w,/assets/img/lewm/probe_pearson_r_by_horizon-800.webp 800w,/assets/img/lewm/probe_pearson_r_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/probe_pearson_r_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Probe Pearson r between predicted and true targets vs horizon. Correlation decays with horizon for all six probes but does not fall to zero — at horizon 100, MLP block location still shows r ≈ 0.35 — so predicted latents retain coarse state information.</figcaption> </figure> <a class="jumpref" href="#ref-probe_pearson_r_by_horizon">&#8617;&#xfe0e; back to text</a> </div> <p>Reports: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/reports/rollout_probe_report_split_seed42_episodes1000_horizon100_img224.md"><code class="language-plaintext highlighter-rouge">rollout_probe_report_split_seed42_episodes1000_horizon100_img224.md</code></a> / <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/reports/rollout_probe_report_split_seed42_episodes1000_horizon100_img224.json"><code class="language-plaintext highlighter-rouge">.json</code></a>. Horizon-zero probe sanity check behind Table 1: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/reports/probe_test_report_seed42_img224.md"><code class="language-plaintext highlighter-rouge">probe_test_report_seed42_img224.md</code></a> / <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/reports/probe_test_report_seed42_img224.json"><code class="language-plaintext highlighter-rouge">.json</code></a>. Multi-seed probe robustness: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/reports/probe_test_report_split_seed42_probe_seeds0_1_2_3_4_img224.md"><code class="language-plaintext highlighter-rouge">probe_test_report_split_seed42_probe_seeds0_1_2_3_4_img224.md</code></a> / <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/reports/probe_test_report_split_seed42_probe_seeds0_1_2_3_4_img224.json"><code class="language-plaintext highlighter-rouge">.json</code></a> / <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/reports/probe_test_report_split_seed42_probe_seeds0_1_2_3_4_img224_seed_summary.md"><code class="language-plaintext highlighter-rouge">..._seed_summary.md</code></a>.</p> </details> <details> <summary>Horizon-35 physical visualizations</summary> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/horizon35_probe_location_scatters-480.webp 480w,/assets/img/lewm/horizon35_probe_location_scatters-800.webp 800w,/assets/img/lewm/horizon35_probe_location_scatters-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/horizon35_probe_location_scatters.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Probe-predicted (×) vs true (●) agent and block locations at raw horizon 35, 20 sampled episodes. Predicted positions are scattered well away from their true counterparts.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/horizon35_physical_recons-480.webp 480w,/assets/img/lewm/horizon35_physical_recons-800.webp 800w,/assets/img/lewm/horizon35_physical_recons-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/horizon35_physical_recons.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Best 12 predicted-latent physical reconstructions at raw horizon 35. Left of each pair: the real simulator render; right: the state decoded from probes on the predicted latent. Block pose can remain roughly plausible while the agent is visibly displaced.</figcaption> </figure> </details> <details> <summary>Latent target-alignment diagnostics</summary> <div id="fig-latent_per_dim_mse_by_horizon"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/latent_per_dim_mse_by_horizon-480.webp 480w,/assets/img/lewm/latent_per_dim_mse_by_horizon-800.webp 800w,/assets/img/lewm/latent_per_dim_mse_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/latent_per_dim_mse_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Per-dimension squared distance between predicted and true encoded latents vs horizon. Grows from ~0.06 at horizon 15 to ~1.92 by horizon 100, saturating near the ~2.0 level expected for two independent latents.</figcaption> </figure> <a class="jumpref" href="#ref-latent_per_dim_mse_by_horizon">&#8617;&#xfe0e; back to text</a> </div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/latent_sq_l2_by_horizon-480.webp 480w,/assets/img/lewm/latent_sq_l2_by_horizon-800.webp 800w,/assets/img/lewm/latent_sq_l2_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/latent_sq_l2_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Total mean squared L2 error between predicted latent and encoded ground truth vs horizon (the same quantity, summed over dimensions rather than averaged), saturating near 365.</figcaption> </figure> <p>Report: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/latent_diagnostics/mse_to_encoded_ground_truth/pusht_expert_train_pusht_lewm_split_seed42_episodes1000_horizon100_img224_latent_mse_report.json"><code class="language-plaintext highlighter-rouge">..._latent_mse_report.json</code></a>.</p> </details> <details> <summary>Empirical-manifold diagnostics</summary> <div id="fig-manifold_min_per_dim_mse"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/manifold_min_per_dim_mse-480.webp 480w,/assets/img/lewm/manifold_min_per_dim_mse-800.webp 800w,/assets/img/lewm/manifold_min_per_dim_mse-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/manifold_min_per_dim_mse.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Per-dimension nearest-neighbor distance to the empirical train/val encoded-latent set. Predicted latents (blue) stay several times farther from the manifold than true future latents (orange).</figcaption> </figure> <a class="jumpref" href="#ref-manifold_min_per_dim_mse">&#8617;&#xfe0e; back to text</a> </div> <div id="fig-manifold_min_sq_l2"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/manifold_min_sq_l2-480.webp 480w,/assets/img/lewm/manifold_min_sq_l2-800.webp 800w,/assets/img/lewm/manifold_min_sq_l2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/manifold_min_sq_l2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Nearest-neighbor squared-L2 distance to the encoded manifold for predicted vs true future latents — the absolute-scale version of the diagnostic above.</figcaption> </figure> <a class="jumpref" href="#ref-manifold_min_sq_l2">&#8617;&#xfe0e; back to text</a> </div> <div id="fig-manifold_drift_ratio"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/manifold_drift_ratio-480.webp 480w,/assets/img/lewm/manifold_drift_ratio-800.webp 800w,/assets/img/lewm/manifold_drift_ratio-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/manifold_drift_ratio.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Ratio of predicted-to-true nearest-manifold distance vs horizon. Peaks near 11× around horizon 25 and falls to about 4.5× by horizon 100; the dashed line at 1.0 marks parity.</figcaption> </figure> <a class="jumpref" href="#ref-manifold_drift_ratio">&#8617;&#xfe0e; back to text</a> </div> <p>Report: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/latent_diagnostics/min_distance_to_manifold/pusht_expert_train_pusht_lewm_split_seed42_episodes1000_horizon100_img224_reftrain_val_manifold_distance_report.json"><code class="language-plaintext highlighter-rouge">..._reftrain_val_manifold_distance_report.json</code></a>.</p> </details> <details> <summary>Norm diagnostics</summary> <div id="fig-norm_pred_vs_true_by_horizon"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/norm_pred_vs_true_by_horizon-480.webp 480w,/assets/img/lewm/norm_pred_vs_true_by_horizon-800.webp 800w,/assets/img/lewm/norm_pred_vs_true_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/norm_pred_vs_true_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Predicted vs true future latent L2 norm vs horizon. Both stay within a narrow band (~13.6–14.4); the predicted norm tracks the encoder shell rather than collapsing or exploding.</figcaption> </figure> <a class="jumpref" href="#ref-norm_pred_vs_true_by_horizon">&#8617;&#xfe0e; back to text</a> </div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/norm_pred_reference_band-480.webp 480w,/assets/img/lewm/norm_pred_reference_band-800.webp 800w,/assets/img/lewm/norm_pred_reference_band-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/norm_pred_reference_band.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Predicted and true future norms against the train/val reference norm band (mean ± std). The predicted norm stays well inside the reference band throughout rollout.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/norm_ratio_by_horizon-480.webp 480w,/assets/img/lewm/norm_ratio_by_horizon-800.webp 800w,/assets/img/lewm/norm_ratio_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/norm_ratio_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Mean ratio of predicted to true latent norm vs horizon — within roughly 0.98–1.01 at every horizon.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/norm_zscore_by_horizon-480.webp 480w,/assets/img/lewm/norm_zscore_by_horizon-800.webp 800w,/assets/img/lewm/norm_zscore_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/norm_zscore_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Latent-norm z-score against the train/val reference vs horizon. The predicted norm z-score stays between about −0.33 and +0.49, i.e. indistinguishable from a normal encoder output by magnitude.</figcaption> </figure> <p>Report: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/latent_diagnostics/norm_trajectory/pusht_expert_train_pusht_lewm_split_seed42_episodes1000_horizon100_img224_reftrain_val_norm_trajectory_report.json"><code class="language-plaintext highlighter-rouge">..._reftrain_val_norm_trajectory_report.json</code></a>.</p> </details> <details> <summary>Velocity and straightness diagnostics</summary> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/latent_velocity_norm_by_horizon-480.webp 480w,/assets/img/lewm/latent_velocity_norm_by_horizon-800.webp 800w,/assets/img/lewm/latent_velocity_norm_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/latent_velocity_norm_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Step-to-step velocity norm of predicted vs true encoded latents by horizon. The true latent moves several times faster than the predicted latent at every horizon. (Also shown in the main text.)</figcaption> </figure> <div id="fig-straightness_pred_vs_true_by_horizon"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/straightness_pred_true_by_horizon-480.webp 480w,/assets/img/lewm/straightness_pred_true_by_horizon-800.webp 800w,/assets/img/lewm/straightness_pred_true_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/straightness_pred_true_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Temporal straightness (mean cosine between consecutive velocity vectors) vs horizon. The predicted trajectory is straighter (~0.70) than the true encoded trajectory (~0.53) at every horizon.</figcaption> </figure> <a class="jumpref" href="#ref-straightness_pred_vs_true_by_horizon">&#8617;&#xfe0e; back to text</a> </div> <div id="fig-straightness_cosine_histogram"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/straightness_cosine_histogram-480.webp 480w,/assets/img/lewm/straightness_cosine_histogram-800.webp 800w,/assets/img/lewm/straightness_cosine_histogram-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/straightness_cosine_histogram.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Distribution of cosines between consecutive velocity vectors. Predicted velocities pile up near 1.0 (over-straight); true velocities spread more broadly around 0.5–0.7.</figcaption> </figure> <a class="jumpref" href="#ref-straightness_cosine_histogram">&#8617;&#xfe0e; back to text</a> </div> <p>Report: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/latent_diagnostics/temporal_straightness/pusht_expert_train_pusht_lewm_split_seed42_episodes1000_horizon100_img224_temporal_straightness_report.json"><code class="language-plaintext highlighter-rouge">..._temporal_straightness_report.json</code></a>.</p> </details> <details> <summary>Systematic-bias diagnostics</summary> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/bias_norm_by_horizon-480.webp 480w,/assets/img/lewm/bias_norm_by_horizon-800.webp 800w,/assets/img/lewm/bias_norm_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/bias_norm_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Norm of the systematic (episode-averaged) error vector vs horizon. It peaks around 3.2 near horizon 75–80, then declines.</figcaption> </figure> <div id="fig-bias_vs_total_per_dim_error"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/bias_vs_total_per_dim_error-480.webp 480w,/assets/img/lewm/bias_vs_total_per_dim_error-800.webp 800w,/assets/img/lewm/bias_vs_total_per_dim_error-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/bias_vs_total_per_dim_error.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Systematic-bias per-dimension energy vs total per-dimension MSE. The shared bias component is tiny relative to total error at every horizon.</figcaption> </figure> <a class="jumpref" href="#ref-bias_vs_total_per_dim_error">&#8617;&#xfe0e; back to text</a> </div> <div id="fig-bias_fraction_by_horizon"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/bias_fraction_by_horizon-480.webp 480w,/assets/img/lewm/bias_fraction_by_horizon-800.webp 800w,/assets/img/lewm/bias_fraction_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/bias_fraction_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Fraction of total error energy explained by the shared systematic bias — roughly 2–5% throughout. Most error is episode-specific, not a single shared drift direction.</figcaption> </figure> <a class="jumpref" href="#ref-bias_fraction_by_horizon">&#8617;&#xfe0e; back to text</a> </div> <p>Report: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/latent_diagnostics/systematic_bias_direction/pusht_expert_train_pusht_lewm_split_seed42_episodes1000_horizon100_img224_systematic_bias_report.json"><code class="language-plaintext highlighter-rouge">..._systematic_bias_report.json</code></a>.</p> </details> <details> <summary>Teacher-forced controls</summary> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio-480.webp 480w,/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio-800.webp 800w,/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/teacher_forced_vs_open_loop_velocity_ratio.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Predicted/true velocity ratio under teacher forcing (~0.33) vs open-loop rollout (~0.16) by horizon. (Also shown in the main text.)</figcaption> </figure> <div id="fig-tf_one_step_error_by_horizon"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/tf_one_step_error_by_horizon-480.webp 480w,/assets/img/lewm/tf_one_step_error_by_horizon-800.webp 800w,/assets/img/lewm/tf_one_step_error_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/tf_one_step_error_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Teacher-forced one-step per-dimension error vs open-loop per-dimension error. Teacher-forced stays small and flat (~0.1); open-loop grows to ~1.9 — feedback compounds the local error.</figcaption> </figure> <a class="jumpref" href="#ref-tf_one_step_error_by_horizon">&#8617;&#xfe0e; back to text</a> </div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lewm/tf_velocity_by_horizon-480.webp 480w,/assets/img/lewm/tf_velocity_by_horizon-800.webp 800w,/assets/img/lewm/tf_velocity_by_horizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lewm/tf_velocity_by_horizon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Mean latent velocity for true (≈5–6), teacher-forced predicted (≈1.85), and open-loop predicted (≈1.0) trajectories. Under-amplification is already present under teacher forcing, before any open-loop feedback.</figcaption> </figure> <p>Report: <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/latent_diagnostics/teacher_forced_velocity/teacher_forced_velocity_report_split_seed42_episodes1000_horizon100_img224.json"><code class="language-plaintext highlighter-rouge">teacher_forced_velocity_report_split_seed42_episodes1000_horizon100_img224.json</code></a> / <a href="https://github.com/kev-hanwen-yang/le-wm/blob/main/diagnostic_results/latent_diagnostics/teacher_forced_velocity/teacher_forced_velocity_report_split_seed42_episodes1000_horizon100_img224.md"><code class="language-plaintext highlighter-rouge">.md</code></a>.</p> </details> <p>Code, split configuration, probe and rollout scripts, raw JSON reports, and plotting scripts are in the accompanying <a href="https://github.com/kev-hanwen-yang/le-wm">GitHub repository</a>; the full diagnostic report set lives under <a href="https://github.com/kev-hanwen-yang/le-wm/tree/main/diagnostic_results"><code class="language-plaintext highlighter-rouge">diagnostic_results/</code></a>.</p> <p><strong>References</strong></p> <ol> <li>LeWM — <em>LeWorldModel: Stable End-to-End Joint-Embedding Predictive Architecture from Pixels.</em></li> <li>V-JEPA 2 / V-JEPA 2-AC — <em>Self-Supervised Video Models Enable Understanding, Prediction and Planning.</em></li> <li><em>Hierarchical Planning with Latent World Models.</em></li> <li>LiveWorld — <em>Simulating Out-of-Sight Dynamics in Generative Video World Models.</em></li> </ol> <script>
(function () {
  function flash(el) {
    el.classList.remove('flash-highlight');
    void el.offsetWidth;                 // force reflow so the animation restarts
    el.classList.add('flash-highlight');
    setTimeout(function () { el.classList.remove('flash-highlight'); }, 2000);
  }
  // Smooth-scroll the target to center, then flash. Re-centering corrections are
  // issued ONLY after the page has stopped scrolling (never per-frame), so we
  // never stack competing smooth-scroll animations — that was what caused lag.
  function centerThenFlash(target) {
    var lastY = null, still = 0, corrections = 0, tries = 0;
    function step() {
      tries++;
      var y = window.pageYOffset;
      still = (lastY !== null && Math.abs(y - lastY) < 1.5) ? still + 1 : 0;
      lastY = y;
      if (still >= 4 || tries > 600) {          // scrolling has stopped (or timeout)
        var vh = window.innerHeight || document.documentElement.clientHeight;
        var r = target.getBoundingClientRect();
        var off = (r.top + r.height / 2) - vh / 2;
        // lazy-loaded images may have shifted the target; correct once, then
        // wait for that scroll to settle before checking again
        if (Math.abs(off) > 24 && corrections < 5 && tries <= 600) {
          corrections++; still = 0; lastY = null;
          window.scrollBy({ top: off, behavior: 'smooth' });
        } else {
          flash(target);
          return;
        }
      }
      requestAnimationFrame(step);
    }
    requestAnimationFrame(step);
  }
  document.addEventListener('click', function (e) {
    var link = e.target.closest && e.target.closest('a.jumpref');
    if (!link) return;
    e.preventDefault();
    var id = decodeURIComponent(link.getAttribute('href').slice(1));
    var target = document.getElementById(id);
    if (!target) return;
    // When jumping TO a figure, point that figure's "back to text" at the exact
    // reference clicked (an image may be referenced from several places).
    if (id.indexOf('fig-') === 0) {
      var src = link.previousElementSibling;
      while (src && !(src.id && src.id.indexOf('ref-') === 0)) src = src.previousElementSibling;
      if (src) {
        var back = target.querySelector('a.jumpref[href^="#ref-"]');
        if (back) back.setAttribute('href', '#' + src.id);
      }
    }
    for (var el = target; el; el = el.parentElement) {
      if (el.tagName === 'DETAILS') el.open = true;   // open any collapsed <details>
    }
    requestAnimationFrame(function () {
      target.scrollIntoView({ behavior: 'smooth', block: 'center' });
      centerThenFlash(target);
    });
  });
})();
</script> <style>
.flash-highlight {
  border-radius: 8px;
  animation: flash-fade 2s ease-out;
}
@keyframes flash-fade {
  0%, 15% {
    outline: 4px solid rgba(255, 179, 0, 1);
    outline-offset: 4px;
    box-shadow: 0 0 0 5px rgba(255, 179, 0, 0.95), 0 0 36px 16px rgba(255, 179, 0, 0.85);
    background-color: rgba(255, 179, 0, 0.18);
  }
  100% {
    outline: 4px solid rgba(255, 179, 0, 0);
    outline-offset: 4px;
    box-shadow: 0 0 0 5px rgba(255, 179, 0, 0), 0 0 36px 16px rgba(255, 179, 0, 0);
    background-color: rgba(255, 179, 0, 0);
  }
}
</style>]]></content><author><name></name></author><category term="research-notes"/><category term="world-models"/><category term="self-supervised-learning"/><category term="jepa"/><category term="long-horizon-planning"/><summary type="html"><![CDATA[A mechanistic study of how open-loop latent predictions preserve global geometry while losing task-relevant physical state.]]></summary></entry></feed>